import { Translation, TranslationsConfig } from './translations.config';
import { StorageService } from '@stanvanheumen/ngx-storage';
export declare class TranslationsService {
    private storage;
    private config;
    private readonly storageToken;
    private readonly dictionary;
    private readonly defaultDictionary;
    private currentLanguage$;
    constructor(storage: StorageService, config: TranslationsConfig);
    use(language: string): void;
    instant(data: string | Translation): any;
    translate(item: string | Translation): any;
    getLanguage(): string;
    getDictionary(): any[];
    private verify(language);
    private getBrowserLanguage();
    private getLanguageDictionary(language);
    private isLanguageSupported(language);
    private validate();
    private getKeys(object, identifier?);
    private compare(first, second);
    private notify(message);
}
