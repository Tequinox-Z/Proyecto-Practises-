import { OnInit } from '@angular/core';
import { NotificationsService } from '../notifications.service';
import { Notification } from '../notifications.interface';
import { Observable } from 'rxjs/Observable';
export declare class NgxNotificationsListComponent implements OnInit {
    private notifications;
    notifications$: Observable<Notification[]>;
    trackById: (index: number, item: Notification) => string;
    constructor(notifications: NotificationsService);
    ngOnInit(): void;
    onNotificationDestroy(notification: Notification): void;
}
