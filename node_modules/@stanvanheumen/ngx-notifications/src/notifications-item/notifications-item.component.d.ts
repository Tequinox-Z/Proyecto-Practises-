import { ChangeDetectorRef, EventEmitter, NgZone, OnDestroy, OnInit } from '@angular/core';
import { Notification } from '../notifications.interface';
export declare class NgxNotificationsItemComponent implements OnInit, OnDestroy {
    private zone;
    private changeDetection;
    notification: Notification;
    onDestroy: EventEmitter<Notification>;
    private progress;
    private count;
    private steps;
    private speed;
    private timer;
    private startTime;
    private difference;
    constructor(zone: NgZone, changeDetection: ChangeDetectorRef);
    readonly progressWidth: string;
    readonly notificationClasses: {
        [x: string]: boolean;
    };
    readonly progressBarClasses: {
        [x: string]: boolean;
    };
    ngOnInit(): void;
    ngOnDestroy(): void;
    private startTimeout();
    private instance;
}
